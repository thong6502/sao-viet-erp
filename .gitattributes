# Normalize line endings so the harness is byte-stable across Windows and Unix/CI.
# Store text as LF and check it out as LF everywhere — bash chokes on CRLF in *.sh.
* text=auto eol=lf

# PowerShell scripts are Windows-native; keep CRLF on checkout.
*.ps1 text eol=crlf
