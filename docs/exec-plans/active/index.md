# Active Plans

> Placeholder template: this folder is empty because the project source is
> currently EMPTY. Add one plan file per active execution plan when real
> multi-session work begins. See `docs/PLANS.md` for when a plan is required.

Keep one markdown file per active execution plan in this folder.

Suggested filename pattern:

- `YYYY-MM-DD-short-topic.md`

Each active plan should be current enough that a fresh agent session can resume
work from the repository alone.
