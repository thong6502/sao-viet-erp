# Backend (FastAPI) runtime + test dependencies.
# Pinned to known-good versions; bump deliberately and re-run init.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20      # FastAPI form data (login OAuth2 form) — bắt buộc runtime
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10        # PostgreSQL driver (used in Docker/prod)
# Lower-bounds (not exact pins) on the pydantic stack so installing into a shared
# global env does not downgrade other tools that need pydantic>=2.11.
pydantic>=2.11.5,<3
pydantic-settings>=2.10.1,<3
email-validator==2.2.0
PyJWT==2.10.1
bcrypt==4.2.1
reportlab==4.4.5              # Báo giá PDF đối ngoại (spec-09 F6)
openpyxl==3.1.5              # Xuất bảng lương + file chuyển khoản .xlsx (Lương Pha 4c)

# Hạ tầng — import TRỄ trong app/realtime.py, app/locks.py, app/storage.py: thiếu REDIS_URL /
# MINIO_ENDPOINT thì hai gói này không được nạp, nên pytest vẫn chạy offline như cũ.
redis==6.4.0                  # pub/sub SSE (nhiều worker) + khoá chống chạy trùng
boto3==1.40.45                # MinIO/S3 — kho file thay cho mount /static công khai

# Test-only (kept here so init.ps1 / init.sh can run backend tests offline-after-install)
pytest==8.4.2
httpx==0.28.1                  # required by FastAPI/Starlette TestClient
