[pytest]
# The template's own smoke test lives in tests/. It keeps init.sh / init.ps1 green
# and verifies the GAN template structure is intact.
# When you build the real app, add its tests here too, e.g.:
#   testpaths = tests backend/tests
testpaths = tests
